[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "insights-mcp"
version = "0.0.0"
description = "A Model Context Protocol server for Red Hat Insights"
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Florian Schüller", email = "fschulle@redhat.com"},
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.14",
]
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.0",
    "requests",
    "PyJWT",
]

[project.optional-dependencies]
dev = [
    "packaging",
    "pre-commit",
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "pytest-subtests",
    "mypy",
    "types-requests",
    "pylint",
    "autopep8",
    "deepeval",
    "llama-index",
    "llama-index-core",
    "llama-index-tools-mcp",
    "llama-index-llms-openai",
    "llama-index-llms-openai-like",
]

[project.scripts]
insights-mcp = "insights_mcp.server:main"
red-hat-lightspeed-mcp = "insights_mcp.server:main"

[project.urls]
Homepage = "https://github.com/RedHatInsights/insights-mcp"
Repository = "https://github.com/RedHatInsights/insights-mcp"
Issues = "https://github.com/RedHatInsights/insights-mcp/issues"

[tool.setuptools.packages.find]
where = ["src", "scripts"]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.package-data]
insights_mcp = ["assets/*.png"]

[tool.pylint.format]
max-line-length = 120

[tool.pylint.messages_control]
disable = ["fixme"]

[tool.autopep8]
max_line_length = 120

[tool.ruff]
target-version = "py312"
line-length = 120
indent-width = 4

[tool.ruff.lint]
# https://docs.astral.sh/ruff/rules/
select = [
    "E4", "E7", "E9", "F",  # Default rules - Pyflakes (`F`) and a subset of the pycodestyle (`E`)
    "E",                    # pycodestyle rules - superset of pep8 (but still WIP https://github.com/astral-sh/ruff/issues/2402)
    "F",                    # pyflakes
    "I",                    # isort
]
ignore = []

# Allow fix for all enabled rules (when `--fix`) is provided.
fixable = ["ALL"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
pythonpath = ["src", "scripts", ".", "tests"]
testpaths = ["tests", "src"]
# Session event loop lets httpx/OpenAI clients finish aclose between parametrized LLM tests.
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"
markers = [
    "integration: tests that call external APIs (e.g. GitHub API)",
    "instrumentation: structural MCP / wiring checks without LLM behavior assertions",
    "llm: behavioral LLM integration tests (requires test_config.json or MODEL_* env)",
]

[dependency-groups]
dev = [
    "pytest-subtests>=0.14.2",
]

# redesign of fastmcp makes this necessary
# as we want to keep our inheritance
# e.g.
# ImageBuilderMCP → InsightsMCP → FastMCP → AggregateProvider …
[tool.pylint.design]
max-parents = 8
